# Boom3 - Requirements

# AI and API
openai>=1.3.5

# Web UI
flask>=3.0.0
flask-sock>=0.7.0

# Auth / rate limiting (required for production)
flask-limiter>=3.5.0

# Redis backend for multi-worker state sharing (optional but recommended)
# Install with: pip install redis
# redis>=5.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0

# For original system compatibility (if needed)
ttkbootstrap>=1.10.1
pillow>=10.0.0

# Utilities
requests>=2.31.0
python-dotenv>=1.0.0


gunicorn
